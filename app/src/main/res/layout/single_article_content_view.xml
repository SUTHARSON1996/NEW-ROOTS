<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent">

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    android:layout_marginBottom="@dimen/activity_vertical_margin">


    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Android Picasso Image Loader0"
        android:id="@+id/article_heading"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:backgroundTint="@color/articleBar"
        android:gravity="center"
        android:background="@color/articleBar"
        android:textColor="@color/black"
        android:fontFamily="cursive"
        android:padding="@dimen/button_padding"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/article_image"
        android:src="@drawable/logo"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:minHeight="@dimen/min_image_height"
        android:minWidth="@dimen/min_image_width"
        android:layout_margin="@dimen/button_padding"
        android:layout_below="@+id/article_heading"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="            Load images in android using picasso image loader library.Load images in android using picasso image loader library.Load images in android using picasso image loader library."
        android:id="@+id/article_content"
        android:layout_below="@+id/article_image"
        android:layout_centerHorizontal="true" />


</RelativeLayout>
</ScrollView>